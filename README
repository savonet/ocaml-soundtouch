ocaml-soundtouch
==========

This package contains an O'Caml interface for sound stretching library, 
otherwise known as libsoundtouch.

Please read the COPYING file before using this software.

Prerequisites:
==============

- ocaml >= 3.0.6 (haven't tried earlier versions)

- libsoundtouch >= 1.3.0 (haven't tried earlier versions)

- findlib >= 0.8.1 (haven't tried earlier versions)

Compilation:
============

	$ make all

This should build both the native and the byte-code version of the
extension library.

Installation:
=============

	$ make install

This should install the library file (using ocamlfind) in the
appropriate place.

